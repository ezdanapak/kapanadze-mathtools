# ნაბიჯ-ნაბიჯ ინსტრუქცია: GitHub-დან PyPI-მდე

## 📋 მოსამზადებელი ნაბიჯები

### 1. PyPI ანგარიში შექმენი
1. გადადი: https://pypi.org/
2. დააჭირე "Register"
3. შეავსე ფორმა და დაადასტურე ელ-ფოსტა
4. **მნიშვნელოვანი**: ჩართე Two-Factor Authentication (2FA)

### 2. GitHub Repository-ს შექმნა

```bash
# შექმენი ახალი repository GitHub-ზე სახელით: kapanadze-mathtools
# შემდეგ ლოკალურად:

cd kapanadze-mathtools
git init
git add .
git commit -m "Initial commit: kapanadze-mathtools library"
git branch -M main
git remote add origin https://github.com/შენი_username/kapanadze-mathtools.git
git push -u origin main
```

## 🔐 PyPI Trusted Publishing-ის დაყენება

### ნაბიჯი 1: PyPI-ზე Trusted Publisher-ის კონფიგურაცია

1. გადადი: https://pypi.org/manage/account/publishing/
2. დააჭირე "Add a new pending publisher"
3. შეავსე ფორმა:

```
PyPI Project Name: kapanadze-mathtools
Owner: შენი_github_username
Repository name: kapanadze-mathtools
Workflow name: publish.yml
Environment name: pypi  (optional, მაგრამ რეკომენდებულია)
```

4. დააჭირე "Add"

### ნაბიჯი 2: GitHub Environment-ის შექმნა (რეკომენდებული)

1. GitHub repository-ში გადადი: Settings → Environments
2. დააჭირე "New environment"
3. სახელი: `pypi`
4. (Optional) დაამატე Protection rules:
   - ✅ Required reviewers (თუ გინდა დასტურის სისტემა)
   - ✅ Wait timer (დაგეგმილი გამოქვეყნებისთვის)

## 🚀 პირველი Release-ის გამოქვეყნება

### ნაბიჯი 1: ვერსიის განახლება
დარწმუნდი, რომ ვერსია სწორია ყველა ფაილში:
- `setup.py` → version="0.1.0"
- `pyproject.toml` → version = "0.1.0"
- `kapanadze_mathtools/__init__.py` → __version__ = "0.1.0"

### ნაბიჯი 2: GitHub Release-ის შექმნა

1. GitHub repository-ში გადადი "Releases" გვერდზე
2. დააჭირე "Create a new release"
3. შეავსე:
   - **Tag**: `v0.1.0` (ახალი tag)
   - **Release title**: `Version 0.1.0 - Initial Release`
   - **Description**: 
     ```
     🎉 პირველი გამოშვება!
     
     ფუნქციონალი:
     - სტატისტიკური გამოთვლები (mean, median, variance, std_dev)
     - გეომეტრიული გამოთვლები (circle, rectangle, triangle)
     ```
4. დააჭირე "Publish release"

### ნაბიჯი 3: ავტომატური გამოქვეყნება

როცა Release-ს შექმნი, GitHub Actions ავტომატურად:
1. ააშენებს package-ს
2. გამოაქვეყნებს PyPI-ზე
3. შეგიძლია იხილო პროგრესი: Actions → Publish to PyPI

## 📦 ლოკალური ტესტირება (დაყენებამდე)

ბიბლიოთეკის ლოკალურად ტესტირება:

```bash
# 1. Virtual environment შექმენი
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 2. Editable mode-ში დააინსტალირე
pip install -e .

# 3. ატესტე
python examples/demo.py
```

## 🎓 სტუდენტებისთვის გამოყენება

როცა PyPI-ზე გამოქვეყნდება, სტუდენტები შეძლებენ:

### ვარიანტი 1: პირდაპირ დაყენება
```bash
pip install kapanadze-mathtools
```

### ვარიანტი 2: requirements.txt-ით

შექმენი `requirements.txt`:
```
kapanadze-mathtools==0.1.0
```

შემდეგ:
```bash
pip install -r requirements.txt
```

### გამოყენება კოდში:
```python
from kapanadze_mathtools import statistics, geometry

numbers = [85, 90, 78, 92, 88]
print(f"საშუალო: {statistics.mean(numbers)}")
print(f"წრის ფართობი (r=5): {geometry.circle_area(5)}")
```

## 🔄 ახალი ვერსიის გამოქვეყნება

1. **კოდის განახლება** და დაკომიტება
2. **ვერსიის ნომრის განახლება** (მაგ. 0.1.0 → 0.2.0):
   - `setup.py`
   - `pyproject.toml`
   - `kapanadze_mathtools/__init__.py`
3. **ახალი Release შექმენი** GitHub-ზე (tag: v0.2.0)
4. **GitHub Actions ავტომატურად** გამოაქვეყნებს

## ✅ შემოწმება

დარწმუნდი, რომ ყველაფერი მუშაობს:

```bash
# 1. ახალ virtual environment-ში
python -m venv test_env
source test_env/bin/activate

# 2. PyPI-დან დააინსტალირე
pip install kapanadze-mathtools

# 3. ატესტე
python -c "from kapanadze_mathtools import statistics; print(statistics.mean([1,2,3,4,5]))"
```

## 🎯 რჩევები სტუდენტებისთვის

კლასში როცა აჩვენებ:

1. **ჯერ აჩვენე GitHub repository**
   - როგორ არის ორგანიზებული კოდი
   - README.md-ის მნიშვნელობა

2. **შემდეგ აჩვენე PyPI გვერდი**
   - https://pypi.org/project/kapanadze-mathtools/
   - როგორ ითვლება downloads

3. **ბოლოს პრაქტიკული დემო**
   - ერთად დააინსტალირეთ
   - გაუშვით demo.py
   - სცადეთ საკუთარი მაგალითები

## 🐛 ხშირი პრობლემები

**პრობლემა**: "Package already exists on PyPI"  
**გადაწყვეტა**: პროექტის სახელი უნიკალური უნდა იყოს. სცადე: `kapanadze-mathtools-yourname`

**პრობლემა**: GitHub Action failed  
**გადაწყვეტა**: შეამოწმე Actions → Logs. ხშირად ვერსია უკვე არსებობს PyPI-ზე.

**პრობლემა**: Import error  
**გადაწყვეტა**: დარწმუნდი რომ package სახელი და import path სწორია.

---

წარმატებები! თუ კითხვები გაქვს, დამიწერე! 🚀
